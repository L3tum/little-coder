---
title: "Captured runbook"
type: "runbook"
tags: "runbook"
confidence: "medium"
source: "memory-context deterministic turn_end; auto-promoted after 3 matches"
created_at: "2026-05-28T00:41:01.575Z"
updated_at: "2026-05-30T00:00:25.343Z"
---
Prompt: Review the code please. Treat everthing as broken until verified working

Outcome: ## Code review

Reviewed:
- `.pi/extensions/memory-context/index.ts`
- `.pi/extensions/branding/index.ts`

Verified:
- `npm run typecheck` passes.
- LSP diagnostics: no diagnostics for both changed files.

## Findings

### 🟡 Major: Invalid auto-promoted candidates are silently deleted from the queue

In `trackShortTermMatches()`, once `match_count >= AUTO_PROMOTE_MATCHES`, the item is not pushed to `remaining`, even if `validateCandidate(item)` rejects it:

```ts
if ((item.match_count ?? 0) >= AUTO_PROMOTE_MATCHES) {
  const candidate = validateCandidate(item);
  if (candidate) {
    writeAcceptedMemory(...)
  }
  changed = true;
} else {
  remaining.push(item);
}
```

This means a candidat

Validation: npm run typecheck
