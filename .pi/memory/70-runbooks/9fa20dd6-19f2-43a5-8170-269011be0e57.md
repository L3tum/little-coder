---
title: "Runbook from index.ts, app.js, patch-extension-notifications.mjs"
type: "runbook"
tags: "runbook, index.ts, app.js, patch-extension-notifications.mjs"
confidence: "high"
source: "memory-context deterministic turn_end"
created_at: "2026-05-27T00:16:04.712Z"
updated_at: "2026-05-27T00:16:04.712Z"
use_count: 6
last_used_at: "active-day:2"
---
Prompt: The command /pi-inspect doesn't seem to capture the full context, only the system prompt, which makes it hard to reason about what's actually in context. Are the displayed tool definitions all in context? There should be clearer labelling around it as well

Outcome: Implemented the `/pi-inspect` context improvements.

Changes made:
- Captures `systemPromptOptions` from `before_agent_start`, so the dashboard can show structured prompt inputs like selected tools, tool snippets, context files, skills, and guidelines.
- Captures the last `before_provider_request` payload, which is the closest view of the actual model request, including messages and active tool schemas when present.
- Snapshot JSON now includes:
  - `systemPromptOptions`
  - `providerPayload`
- Dashboard labels are clearer:
  - `Context` → `Prompt/context sent to model`
  - `Tools` → `Tool definitions (provider schemas)`
- Dashboard now shows extra context items:
  - `structured prompt input

Validation: node --check scripts/patch-extension-notifications.mjs && npx tsc --noEmit
