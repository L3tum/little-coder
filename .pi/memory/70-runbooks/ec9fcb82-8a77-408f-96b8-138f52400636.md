---
title: "Runbook from index.ts, index.ts, index.ts"
type: "runbook"
tags: "runbook, index.ts, index.ts, index.ts"
confidence: "high"
source: "memory-context deterministic turn_end"
created_at: "2026-05-27T23:53:09.000Z"
updated_at: "2026-05-27T23:53:09.001Z"
---
Prompt: Remove the tools "compatStats", "memoryReview", and only enable the issueAgentDone command in issue-agent mode. The first two tools should only be available as commands to the user.

Outcome: Implemented and verified with `npm run typecheck`.

Changes:
- Removed `compatStats` tool registration; `/compat-stats` remains available as a user command.
- Removed `memoryReview` tool registration; added `/memory-review` user command.
- `issueAgentDone` is now only registered when `ISSUE_AGENT_DONE_FILE` is present, i.e. issue-agent sub-agent mode.

Validation: npm run typecheck
