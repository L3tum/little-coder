---
title: "Decision from index.ts"
type: "decision"
tags: "decision, index.ts"
confidence: "high"
source: "memory-context deterministic turn_end"
created_at: "2026-05-28T00:04:51.266Z"
updated_at: "2026-05-28T00:04:51.267Z"
use_count: 3
last_used_at: "active-day:1"
---
Prompt: The edit tool diff appears double in the chat right now. I think it's because the old "edit-compat" tooling was blocking it before, and the new "edit-custom" unblocked the native diff rendering, but also posts its own diff now.

Outcome: Fixed. The `edit-custom` extension now delegates `renderResult` back to Pi’s native edit renderer instead of rendering its own diff snapshot.

Changed:
- Removed custom `renderDiff` rendering
- Removed unused TUI imports/helpers
- Kept argument normalization and compatibility behavior intact

Verified with:

```bash
npm run typecheck
```

Validation: npm run typecheck
